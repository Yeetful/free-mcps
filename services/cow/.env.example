# All optional — the public CoW order-book API needs no key.

# Override the order-book API host (e.g. a self-hosted orderbook or the
# staging barn: https://barn.api.cow.fi).
# COW_API_URL=https://api.cow.fi

# Per-IP rate limit for the free front door (default 60/min).
# RATE_LIMIT_PER_MINUTE=60
