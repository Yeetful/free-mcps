// Default env for tests. The free service needs no payment config and no API
// key — the CoW order-book API is unauthenticated. COW_API_URL overrides the
// production host (e.g. for the barn/staging orderbook).
