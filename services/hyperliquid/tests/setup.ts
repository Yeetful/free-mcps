// Default env for tests. The free service needs no payment config and no API
// key — the Hyperliquid public API is unauthenticated. HYPERLIQUID_API_URL /
// HYPERLIQUID_WS_URL override the mainnet defaults (e.g. for testnet).
