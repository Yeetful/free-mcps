// Default env for tests. The free service needs no payment config. The
// Snapshot read API needs no key; hub/sequencer URLs default to the public
// ones (SNAPSHOT_HUB_URL / SNAPSHOT_SEQUENCER_URL override).
